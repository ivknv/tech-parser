Things that were already done:
    Store all the articles inside database instead of file

Things that need to be done:
    Current: Use AJAX everywhere
    
    Automatically find RSS feed URL and icon
    
    Write unit tests
    
    Store information in parser modules about arguments they receive
    
    Make writting custom parsers easier
    
    Use POST requests to add/remove articles from history/blacklist
    
    Use CSRF tokens to validate forms
